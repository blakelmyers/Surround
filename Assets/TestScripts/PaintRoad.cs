using UnityEngine;
using System.Collections;

public class PaintRoad : MonoBehaviour {

	bool paitingTerrain = false;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void PaintRoadFunction()
		
	{
		/*
		paintingTerrain = true;
		
		int x = Convert.ToInt32(((transform.position.x - terrainObj.transform.position.x) / terrain.terrainData.size.x) * terrain.terrainData.heightmapWidth);
		
		int y = Convert.ToInt32(((transform.position.z - terrainObj.transform.position.z) / terrain.terrainData.size.z) * terrain.terrainData.heightmapHeight);
		
		
		
		splatmapData[y, x, 0] = 0;
		
		splatmapData[y, x, 1] = 1;
		
		
		
		terrain.terrainData.SetAlphamaps(0, 0, splatmapData);
		*/
		//terrain.Flush();
		
		
		
	}
}
